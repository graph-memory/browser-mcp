services:
  browser-mcp:
    build: .
    ports:
      - "7777:7777"
    environment:
      - BROWSER_MCP_API_KEY=${BROWSER_MCP_API_KEY:-}
    volumes:
      - browser-profiles:/root/.browser-mcp/profiles

volumes:
  browser-profiles:
