services:
  browser-mcp:
    build: .
    ports:
      - "7777:7777"
    environment:
      - BROWSER_MCP_API_KEY=${BROWSER_MCP_API_KEY:-}
    volumes:
      - browser:/home/browser/.browser-mcp

volumes:
  browser:
